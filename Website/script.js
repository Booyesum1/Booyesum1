//This is the class declaration
class User {
    //This is the constructor statement for the class User
    constructor (
        //It defines the perameters of the class User
    firstName,
    lastName,
    email,
    birthDate,
    streetAddress,
    city,
    state,
    zip,
    userName,
    password,
    character,
) {
    //Defines Properties
    this.firstName = firstName,
    this.lastName = lastName,
    this.email = email,
    this.birthDate = birthDate,
    this.streetAddress = streetAddress,
    this.city = city,
    this.state = state,
    this.zip = zip,
    this.userName = userName,
    this.password = password,
    this.character = character

};

}